name: switchsoulmonarch
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.4
  system_tray: ^0.1.0
  sqflite: ^2.0.2+1
  flutter_riverpod: ^1.0.4
  freezed_annotation: ^2.0.3
  window_manager: ^0.2.3
  tray_manager: ^0.1.7

  file_picker:
    git:
      url: https://github.com/kyu-suke/flutter_file_picker.git
      ref: mod/getDirectoryPath

  magnetica:
    git:
      url: https://github.com/kyu-suke/magnetica.git
      ref: main

  hotkey_holder:
    git:
      url: https://github.com/kyu-suke/hotkey_holder.git
      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  build_runner: ^2.1.11
  freezed: ^2.0.3+1

flutter:
  uses-material-design: true
  assets:
    - assets/appIcon/